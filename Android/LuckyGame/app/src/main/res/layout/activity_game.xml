<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_game"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.vasilkoff.luckygame.activity.GameActivity">

    <Button
        android:id="@+id/gamePlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Play Game"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:onClick="onClickGame"/>

    <Button
        android:id="@+id/gameWin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Win"
        android:onClick="onClickGame"
        android:layout_alignBaseline="@+id/gamePlay"
        android:layout_alignBottom="@+id/gamePlay"
        android:layout_toStartOf="@+id/gamePlay" />

    <Button
        android:id="@+id/gameLose"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Lose"
        android:onClick="onClickGame"
        android:layout_alignBaseline="@+id/gamePlay"
        android:layout_alignBottom="@+id/gamePlay"
        android:layout_toEndOf="@+id/gamePlay"
        android:layout_marginStart="11dp" />

</RelativeLayout>
